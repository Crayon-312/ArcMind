# ArcMind V2 Agent 协作入口

本文件是 ArcMind V2 的极薄 Agent（智能协作助手）入口。项目不内置完整开发协作引擎；任务拆解、上下文路由、执行门禁和质量规则由 Agent Context OS（智能协作上下文操作系统）统一管理。

Agent Context OS 只约束 Codex（代码协作工具）等开发工具如何维护本仓库，不属于 ArcMind 产品运行时，不得混入实时语音、云端主 Agent 或工作机执行链路。

## 启动顺序

1. 读取本文件。
2. 读取 `.agent-context/config.json`。
3. 按 `engine.source` 和 `engine.version` 加载指定版本的 Agent Context OS。
4. 读取 `docs/00-index.md`，按任务类型选择项目文档。
5. 按 `memory.source_paths` 检索项目记忆源；本地索引缺失时直接读取源文件。
6. 按当前协作引擎判定变更等级并执行对应门禁。

## 项目阶段

- 当前长期开发分支为 `v2`；`main` 是已冻结的旧版项目。
- V2 是不继承旧代码和旧文档事实的全新项目。
- 当前阶段为文档与架构建模，未经用户明确要求不得提前创建业务代码骨架。

## 硬规则

- 本仓库只保存极薄入口、项目配置、项目文档和可审查的项目记忆源，不复制完整 Agent Context OS。
- 项目事实以用户确认、当前代码与测试、当前项目文档和项目记忆源为依据；外部方案和 Agent 推测不能直接标记为当前事实。
- `docs/` 同时是 Obsidian 知识库；Git 中的 Markdown 文件是事实源，Obsidian 只提供阅读、链接和检索界面。
- 正式文档必须进入对应领域唯一命名的 `00-*-map.md` 内容地图，并用有语义的 Markdown 链接连接依据、细化、实现、决策、计划或验收；当前有效和已接受文档不得成为关系孤岛。
- 每项非平凡变更在 `docs/plans/` 使用一个方案落实台账作为任务舱。纯讨论只能修改状态为 `draft` 的任务舱，不得修改正式文档、代码或配置。
- 任务舱按 `draft → confirmed → active → done` 流转；只有用户确认后的完整方案和有序任务清单才能进入执行，实施阶段必须按清单 ID、依赖和验收口径推进。
- 正式文档不预先描述尚未验证的实现。任务舱先确定目标契约，代码与正式文档在同一任务内完成，验证通过后一起发布为当前事实；纯文档任务直接以文档验收形成闭环。
- Git worktree 不是每任务默认配置；仅在并行任务、长周期或高风险改造、实验验证、主工作区不干净或需要独立运行态时启用，并在任务舱记录原因。
- Agent Context OS 与 ArcMind Runtime 是两个不同系统，任何设计和命名都必须明确区分开发协作与产品运行时职责。
- 涉及产品、交互、业务、架构、实现习惯或历史问题时，必须先检索相关项目记忆，再读取证据文档。
- 本地向量索引和全文索引只是可重建缓存，不是事实源；统一放在 `.agent-context/local-index/`，不得提交到 Git（版本控制工具）。
- 新增稳定事实时同步更新项目文档和对应记忆；不确定内容必须标为 `draft`、`assumption` 或开放问题。
- 账号、密钥、访问令牌、会话凭据、真实私密对话、语音样本和本机隐私路径不得写入仓库。
- 产品运行时未来必须保持实时语音供应商可替换；不得将对话供应商直接等同于任务编排器或工作机执行器。
- 跨模块修改必须通过公开契约；禁止手机端、云端和工作机端绕过边界直接依赖彼此内部实现。
- 修改后必须运行 `scripts/check-agent.ps1`，并执行 `.agent-context/config.json` 中适用于当前阶段的验证命令。
- 文档或配置修改后，如仓库存在可运行开发态则必须重启；当前纯文档阶段没有开发服务时应明确记录“不适用”。
- 完成修改并通过验证后默认创建本地提交；推送远端必须有用户明确授权。

## 文档路由

- 产品方向、需求或范围：读取 `docs/product/`。
- 跨端职责、数据边界或技术方案：读取 `docs/architecture/`。
- 对话、任务、提醒等业务流程：读取 `docs/business/`。
- 跨端消息和公开接口：读取 `docs/contracts/`。
- 已确认的重要取舍：读取 `docs/decisions/`。
- 阶段任务与验收状态：读取 `docs/plans/` 和 `docs/quality/`。

## 最终报告

最终回复至少说明：

- 使用的协作引擎来源和版本。
- 已读项目文档与检索的记忆源。
- 本地索引状态。
- 变更等级和修改范围。
- 验证结果与运行态状态。
- 项目记忆的新增、更新、冲突或开放问题。
- 本地提交状态和是否推送。
