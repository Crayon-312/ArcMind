---
type: task-capsule
status: active
task_id: "0009"
created: 2026-08-06
updated: 2026-08-06
change_level: S3
---

# 方案落实台账：第一阶段开发入口门禁

## 关系导航

- 所属领域：[计划内容地图](00-plans-map.md)
- 实施顺序：[V2 分步实施顺序](../product/05-build-sequence.md)
- 技术入口：[技术选型与决策门禁](../architecture/08-technology-selection.md)
- 安全部署：[安全与部署边界](../architecture/07-security-and-deployment.md)
- 身份流程：[身份与工作机绑定](../business/04-identity-and-device-flow.md)
- 公开契约：[云端公开 API 契约草案](../contracts/02-cloud-public-api.md)
- 验收门禁：[第一阶段设计就绪验收](../quality/02-phase-1-design-acceptance.md)

## 目标与边界

### 业务目标

关闭第一阶段设计就绪门禁中的 DR-004 至 DR-007，为三应用工程骨架和第一条身份纵向切片提供足够明确、可验证且不过度扩展的设计依据。

### 本次包含

- 定稿“本人优先”的邮箱验证码身份、会话、恢复、撤销和最低限速规则。
- 验证固定公网 IP（互联网协议地址）直接提供可信 HTTPS（安全超文本传输协议）的可行性和恢复方式。
- 定稿文字增量响应、任务进度和重连所需的 SSE（服务器发送事件）通道与错误语义。
- 选择首个主 Agent（智能任务编排助手）模型供应商和最小失败降级方式。
- 将身份、事件和错误契约转换为 OpenAPI（开放接口描述规范）3.1 与 JSON Schema（JSON 数据结构规范）。
- 补齐身份与文字闭环的页面状态和测试矩阵，重新判定第一阶段门禁。

### 本次不包含

- 不创建三应用工程骨架或业务代码。
- 不实现实时语音、工作机连接、长期记忆、提醒或公开注册体系。
- 不设计组织、角色、多租户、管理员后台或复杂账号治理。
- 不把隔离 VPS（虚拟专用服务器）验证结果或尚未完成的真实手机行为写成 ArcMind 已部署运行事实。
- 不因“仅本人使用”取消会话保护、验证码一次性消费、限速或审计基线。

## 证据与判断

### 已确认事实

- 用户确认首版只保障本人使用，但保留普通个人账户边界；不做组织、角色、多租户和公开注册治理。
- 用户确认使用邮箱短时一次性验证码，不使用密码作为首版登录凭据。
- 用户当前没有域名，希望优先使用“可信 HTTPS + 公网 IP”访问。
- Let’s Encrypt（免费公开证书颁发机构）于 2026-01-15 宣布 IP 地址证书正式可用，支持 IPv4 和 IPv6，证书有效期为 160 小时；IP 地址证书必须使用 `shortlived`（短期证书）配置档。
- Let’s Encrypt 对 IP 地址只支持 `http-01` 或 `tls-alpn-01` 两种 ACME（自动证书管理环境）所有权校验，不支持依赖域名系统的 `dns-01`。
- Caddy（自动 HTTPS 反向代理）当前稳定版 v2.11.4 暴露 ACME `profile` 配置，并允许 Let’s Encrypt 签发公网 IP 证书；IP 地址仍需显式配置公开证书签发器，不能依赖默认的本地自签名行为。
- 浏览器中的麦克风等强能力要求安全上下文；远程资源通常必须通过浏览器认可的 HTTPS 提供。安全 Cookie（浏览器会话凭据）可在 HTTPS 下使用，省略 `Domain` 属性时形成仅当前主机可用的 Cookie。
- 目标 VPS 已通过独立 Caddy 容器完成公网 IPv4 证书签发、TLS 1.3、IP 匹配、证书链、HTTP 200、无 SNI 修正和续期窗口验证；真实手机与一次实际续期仍未完成，因此 DR-005 保持部分通过。

### 合理推断

- “公网 IP + Let’s Encrypt 短期证书 + Caddy 自动续期”的技术路径已经成立；正式部署仍要求固定 IP 和唯一边缘代理，不能与现有业务争抢 80/443。
- 该路径不需要购买域名，但证书只有 160 小时，续期、证书存储持久化和到期监控属于可用性硬要求；其运维容错低于普通域名证书。
- 手机 Web 与 API（应用程序接口）保持同源部署，可避免跨域会话配置，并允许使用不带 `Domain` 的 `__Host-` 前缀 Cookie。
- 本人优先最简单的注册边界是部署配置中只允许一个归一化邮箱；接口仍保持普通个人用户与多会话模型，未来增加用户不需要重写身份主体。

### 假设与开放问题

- 目标 VPS 当前公网 IPv4 已完成证书签发，但该地址是否由供应商保证长期固定仍需在正式部署前确认；80 端口属于现有业务，ArcMind 不得争抢或修改。
- 目标网络允许外部通过 443 直达本次隔离验证容器；正式共用该公网入口时仍需设计唯一边缘代理，不能让两个容器同时绑定 443。
- 生产邮件通过哪一个 SMTP（简单邮件传输协议）中继发送；仓库只定义标准适配器，不保存真实账号和密钥。
- 首个主 Agent 模型已选择 DeepSeek V4 Pro；生产凭据、真实中文质量、流式取消、工具调用和结构化输出仍待实测。
- 公网 IP 证书已签发并观察到续期窗口；一次真实自动续期、告警和手机兼容仍待验证，失败时的可信主机名替代路径需由用户另行确认。

## 待确认的最小方案

### 身份与会话

- 只允许部署配置中的一个归一化邮箱发起验证码；对外响应不暴露邮箱是否被允许或已存在。
- 验证码为 6 位数字、10 分钟过期、最多失败 5 次、成功后立即消费；同一邮箱签发新挑战时使旧挑战失效，服务端只保存验证码摘要。
- 发送限速初值：同一邮箱 15 分钟最多 3 次，同一来源 IP 15 分钟最多 10 次；验证失败受挑战次数和来源 IP 双重限制。数值通过配置调整，但生产环境不得关闭。
- 登录成功后签发至少 256 位随机会话令牌；数据库只保存令牌摘要。Cookie 使用 `Secure`、`HttpOnly`、`SameSite=Lax`、`Path=/`，名称使用 `__Host-arcmind_session`，不设置 `Domain`。
- 会话允许本人多个浏览器同时在线；空闲 7 天失效、绝对 30 天失效。支持退出当前会话和撤销全部会话，撤销同时终止对应 SSE 长连接。
- 首版身份恢复等同于重新通过邮箱验证码登录；邮箱本身失去控制时不提供绕过邮箱的后门恢复。重新登录后提供“撤销全部其他会话”。
- 手机 Web 与 API 同源部署，并对改变状态的请求校验 `Origin`（请求来源）和幂等标识；不开放任意跨域携带会话凭据。
- 开发环境使用邮件捕获器或测试适配器；生产环境使用标准 SMTP 适配器和外部中继，真实凭据只进入部署环境。

### 公网 IP 与 HTTPS

- 首版只承诺一个固定公网 IPv4；IPv6 仅在目标 VPS 确实提供稳定地址并单独通过验证后启用。
- 反向代理锁定到 Caddy v2.11.4 或经验证的更高兼容版本，显式选择 Let’s Encrypt ACME 服务和 `shortlived` 配置档。
- 公网只暴露 80/443；应用、数据库和内部管理端口不直接暴露。Caddy 证书、账户和续期状态使用持久卷保存。
- 部署验收必须覆盖：生产证书链可信、IP 地址匹配、HTTP 自动跳转 HTTPS、手机浏览器无证书警告、`isSecureContext` 为真、Secure Cookie 正常、麦克风权限可请求。
- 续期验收必须覆盖：重启后证书状态保留、短期证书进入自动续期窗口、续期失败可观察、剩余有效期低于 48 小时时告警。
- 任一硬条件失败时不得退化到忽略证书警告或明文 HTTP；任务回到 `confirmed` 状态，由用户选择可信主机名替代路径。

### 文本事件与模型

- 文字请求使用普通 HTTPS 创建用户轮次和响应记录，增量输出通过经 Cookie 鉴权的 SSE 返回；最终轮次必须持久化，SSE 不是事实源。
- 每个事件包含稳定事件 ID、事件类型、会话 ID、响应 ID、序号和时间；客户端使用 `Last-Event-ID` 或显式游标重连，重复事件按 ID 去重。
- 首版事件至少覆盖 `response.started`、`response.delta`、`response.completed`、`response.failed` 和心跳；任务阶段再复用同一事件信封增加任务进度类型。
- 断线后允许丢弃未持久化的历史增量，但必须能查询最终轮次；生成仍在进行时，重连从服务端保留的有限事件窗口继续，窗口外返回可识别的“重新同步”错误。
- 模型适配器只暴露生成、取消、超时、限流和供应商错误归一化；首个供应商失败时保存用户轮次和失败事实，允许用户重试，不在首版自动切换第二家模型造成重复计费或语义漂移。
- 首个供应商必须通过官方接口、目标网络连通、中文效果、流式取消、结构化输出、价格和数据路径核查后才能写入正式决策。

## 用户批准的方案

- 2026-08-06，用户批准按本任务舱的最小身份参数、公网 IP 证书验证路径、SSE 基线和 G1-G10 清单继续。
- 用户提供一台 Ubuntu VPS 用于固定公网 IP 和 HTTPS 验证；连接凭据只用于当次操作，不写入仓库、项目文档、日志摘要或项目记忆。
- 该 VPS 已运行另一套容器化服务，验证必须与现有 Caddy、API 和数据组件隔离，不得覆盖或重启现有业务。
- 未授权边界保持不变：不创建 ArcMind 业务代码骨架，不推送远端，不把未通过真机或供应商验证的行为标为当前事实。

## 系统影响矩阵

| 影响类型 | 内容 | 计划写回位置 | 发布条件 |
|---|---|---|---|
| 身份 | 本人邮箱验证码、会话、恢复、撤销和限速 | 身份模块、业务流程、公开契约、身份决策 | 字段、状态、错误和验收完整 |
| 部署 | 固定公网 IP、短期公开证书、续期和恢复 | 安全部署、手机 Web、部署决策 | 目标 VPS 与真实手机验证通过 |
| 事件 | 文字增量、重连、去重和最终事实恢复 | 公开契约、跨端事件、事件决策 | 重连和故障用例可判定 |
| 模型 | 首个供应商、适配器和失败语义 | 技术选型、Agent 架构、模型决策 | 官方资料与目标网络实测通过 |
| 机器契约 | OpenAPI 3.1 与 JSON Schema | `packages/contracts` 的未来契约源、契约文档 | 机器校验和兼容规则明确 |
| 质量 | 页面状态、测试矩阵和门禁结果 | 第一阶段设计就绪验收 | DR-004 至 DR-007 均有证据结论 |
| 项目记忆 | 关闭身份、HTTPS、事件和模型开放问题 | `.agent-context/memory-sources/` | 与正式决策和验收一致 |

## 有序任务清单

实施只依据本清单，不依赖聊天上下文补全细节。范围变化时先更新本任务舱并重新确认。

| 顺序 | ID | 任务 | 依赖 | 验收口径 | 状态 |
|---:|---|---|---|---|---|
| 1 | G1 | 用户确认最小身份参数、IP 证书路径、SSE 基线和清单 | 当前草稿 | 批准范围和未授权边界写入本任务舱 | done |
| 2 | G2 | 定稿身份领域、状态、错误、会话撤销和 SMTP 适配边界 | G1 | DR-004 条件逐项有文档和验收用例 | done |
| 3 | G3 | 在目标 VPS 验证固定公网 IP、Caddy 短期证书、持久化和手机安全上下文 | G1 | 签发、重启、续期观测、Cookie 和麦克风均有证据 | active |
| 4 | G4 | 定稿 SSE 事件信封、重连游标、取消和错误模型 | G2 | DR-006 条件逐项可由契约测试验证 | done |
| 5 | G5 | 核对并选择首个主 Agent 模型供应商 | G1 | 网络、流式、取消、结构化输出、成本和数据路径有证据 | active |
| 6 | G6 | 形成身份、HTTPS、事件和模型决策记录并同步正式架构 | G2-G5 | 只记录已确认设计和已验证部署事实 | active |
| 7 | G7 | 细化 OpenAPI、JSON Schema、页面状态和测试矩阵 | G4-G6 | 身份与文字闭环不存在未说明字段、状态或错误 | active |
| 8 | G8 | 重新执行第一阶段设计门禁评审 | G7 | DR-004 至 DR-007 和必需设计产物全部可追溯 | pending |
| 9 | G9 | 同步内容地图、项目记忆、验证结果和发布记录 | G8 | 无孤立文档、记忆冲突或未标注假设 | pending |
| 10 | G10 | 运行项目检查并创建本地提交 | G9 | 项目门禁和差异检查通过，工作区提交范围单一 | pending |

## 文档与代码同步策略

- 本任务是纯设计与部署验证任务，先形成可执行契约和门禁证据，不创建业务代码骨架。
- `draft` 阶段只修改本任务舱；用户批准后才更新正式决策和架构文档。
- 正式文档可以声明已确认的目标契约，但不得声明尚未实现的运行行为已经可用。
- 三应用骨架和身份纵向切片另建实施任务舱；实现、契约、测试和正式运行文档在对应任务内一起验收发布。

## 工作区模式

- 当前选择：继续在 `v2` 当前工作区串行维护任务舱，不创建 Git worktree（Git 独立工作树）。
- 理由：当前只新增一个草稿文档，没有并行实现、独立运行态或共享文件竞争。
- 当 G3 需要在 VPS 做证书实验时，使用服务器独立临时部署目录和测试配置，不污染正式运行配置；若本地同时开始其他实现任务，再按并行条件启用 worktree。

## 官方证据

- [Let’s Encrypt：6-day and IP Address Certificates are Generally Available](https://letsencrypt.org/2026/01/15/6day-and-ip-general-availability)
- [Let’s Encrypt：Announcing Six Day and IP Address Certificate Options in 2025](https://letsencrypt.org/2025/01/16/6-day-and-ip-certs/)
- [Caddy：Automatic HTTPS](https://caddyserver.com/docs/automatic-https)
- [Caddy v2.11.4 发布页](https://github.com/caddyserver/caddy/releases/tag/v2.11.4)
- [Caddy v2.11.4 ACME 签发器源码](https://github.com/caddyserver/caddy/blob/v2.11.4/modules/caddytls/acmeissuer.go)
- [MDN（Mozilla 开发者网络）：Secure contexts](https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Secure_Contexts)
- [MDN：MediaDevices.getUserMedia](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia)
- [MDN：Using HTTP cookies](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Cookies)

## 执行记录

| 时间 | 任务 ID | 动作与证据 | 结果 |
|---|---|---|---|
| 2026-08-06 | 调研 | 核对 Let’s Encrypt、Caddy 和浏览器官方资料；读取身份、部署、契约、门禁和项目记忆 | 公网 IP 证书路径在技术上可行，但尚缺目标 VPS 与真机证据 |
| 2026-08-06 | 建舱 | 将用户已确认选择、最小方案、开放问题和依赖清单写入草稿任务舱 | 等待用户确认 G1，不修改正式事实 |
| 2026-08-06 | 验证 | 运行 `scripts/check-agent.ps1` 和 `git diff --check` | 文档、链接、内容地图、JSONL（逐行 JSON）和差异检查通过 |
| 2026-08-06 | G1 | 用户批准任务舱方案并提供目标 VPS | 任务舱进入执行；凭据不落库，现有业务必须保持隔离 |
| 2026-08-06 | G3 准备 | 外部探测与服务器只读检查：22/80 可达、443 未监听；Caddy v2.11.4 运行于现有 Docker Compose（容器组合编排）项目 | 可使用独立临时容器验证 443，不覆盖现有 80 端口服务 |
| 2026-08-06 | G3 | 独立 Caddy 容器只监听 443；签发公开 IPv4 证书并验证 TLS 1.3、IP 匹配、信任链、HTTP 200 和续期窗口 | 服务端路径通过；初次失败根因为 IP 客户端无 SNI，配置 `default_sni` 后修复；等待真实手机与实际续期 |
| 2026-08-06 | G2/G4 | 定稿邮箱挑战、服务端会话、撤销、SSE 快照、重连、取消和稳定错误语义 | 身份和文字事件决策、架构、模块、契约及项目记忆已同步 |
| 2026-08-06 | G5/G6 | 核对官方模型能力、定价与目标网络，选择 DeepSeek V4 Pro 并形成四项正式决策 | 供应商方向已确定；无生产密钥，真实流式取消、工具与结构化输出保持未验证 |
| 2026-08-06 | G7 | 新增 OpenAPI、响应事件 Schema、手机页面状态和测试矩阵；执行解析、引用及正反样例校验 | 机器契约检查通过；G7 仍等待 G5/G6 外部证据闭环后结束 |
| 2026-08-06 | G3 回归复查 | 从本地复查目标入口：80 仍返回既有 Caddy 业务的 HTTP 200；443 TCP 可建立，但 SChannel 与 .NET TLS 握手分别被关闭或超时 | 临时 HTTPS 验证入口当前不可用；未取得服务器状态与日志，根因未确认，手机验收暂停 |
| 2026-08-06 | G3 多节点复查 | Python 分别测试 TLS 1.2/1.3、带 IP SNI、无 SNI 和 443 明文，全部超时；印度、意大利、俄罗斯、瑞典和美国五个公网节点访问 443 也全部超时 | 排除仅本机或本地网络异常，故障范围收敛到服务器或其公网边缘路径 |
| 2026-08-06 | G3 安全访问 | SSH 服务可完成版本交换和密钥交换，但本机没有已验证的目标主机密钥，也没有可用登录私钥 | 必须先由用户从云服务商控制台核对 ED25519 指纹 `SHA256:pTwfJxs6sCEPj2ZFrKIGMqtmMa0kXjRus3SBZ1EKyEI`，再配置临时密钥访问；不绕过主机校验 |
| 2026-08-06 | G3 根因 | 用户重新授权临时密码登录；只读检查发现验证容器持续重启且主机无 443 监听，Caddyfile 第 12 行的检测页双引号破坏 `respond` 参数 | 确认根因是当前临时容器重建时的内联字符串转义缺陷，不是证书、SNI 或公网 IP 方案失效 |
| 2026-08-06 | G3 修复 | 离线解析独立 Caddyfile 后，以主机只读配置、静态页面、证书卷和配置卷替换损坏容器；只重启验证容器一次 | TLS 1.3、IP SAN、可信证书链、无 SNI、HTTP 200、Chrome 安全上下文和重启恢复通过；`cloud-*` 容器 ID 与状态不变 |

## 验证与发布

### 验证结果

- 目标 VPS 的公开证书、TLS、IP 匹配、无 SNI、持久化重启和桌面浏览器安全上下文当前均通过；中途 443 回归已确认并修复为临时检测容器的配置转义缺陷。
- OpenAPI 共 10 个操作的解析、内部引用和操作标识检查通过；会话详情正反样例通过。
- 响应事件 Schema 的 7 类正样例和 4 类反样例通过，事件专属载荷不会串用。
- 当前本机 JSON Schema 校验器未安装日期等格式插件，因此只完成结构断言；格式断言已写入后续契约测试硬门禁，不把本轮结果扩大为格式已验证。
- `scripts/check-agent.ps1` 通过，新文档已进入内容地图且没有形成知识孤岛；最终提交前还需再次运行。
- `git diff --check` 在前一轮通过；最终提交前还需对完整差异再次运行。
- 当前仍是纯文档阶段，没有可重启的业务开发服务，运行态重启不适用。

### 未验证范围与剩余风险

- 未确认目标公网 IP 的供应商固定承诺，也未设计与现有业务共用 443 的唯一生产边缘代理。
- 未验证 iOS Safari、Android Chrome 和目标国产浏览器对该 IP 证书、Cookie 和麦克风的真实行为。
- 未选择生产 SMTP 中继；首个主 Agent 模型已选择，但尚无生产密钥完成真实能力测试。
- 160 小时证书使续期故障的恢复窗口较短；已完成续期窗口观察和容器重启恢复，但在一次实际自动续期与告警实测前不得视为稳定上线方案。

### 发布记录

- 正式文档：已更新身份、HTTPS、模型、事件、数据边界、机器契约、手机页面和测试矩阵；任务舱保持 `active`。
- 代码或配置：未创建业务代码，未修改 ArcMind 运行配置。
- 项目记忆：已同步四项决策、响应快照边界和两项未完成验证。
- 本地提交：完整门禁验证后创建，只包含本任务相关文档与项目记忆。
- 远端推送：未经单独授权不得执行。

## 状态流转

- `draft`：讨论中，只修改任务舱。
- `confirmed`：用户已批准完整方案和清单，可以更新正式设计并执行部署验证。
- `active`：正在按清单实施和验证。
- `done`：门禁通过，正式文档、项目记忆、验证证据和发布记录已闭环。
