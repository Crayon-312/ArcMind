# 业务流程：身份与工作机绑定

状态：draft
最后校验日期：2026-07-30

## 关系导航

- 所属领域：[业务内容地图](00-business-map.md)
- 安全边界：[安全与部署边界](../architecture/07-security-and-deployment.md)
- 责任模块：[身份与访问](../modules/identity-access.md)、[工作机网关](../modules/workstation-gateway.md)
- 公开契约：[云端公开 API 契约草案](../contracts/02-cloud-public-api.md)、[云端与工作机通道契约草案](../contracts/03-workstation-channel.md)

## 已确认业务规则

- 手机端通过邮箱注册并登录云端。
- 工作机端登录或绑定到同一用户主体后，成为该用户的可选扩展设备。
- 没有绑定工作机不影响聊天、记忆、云端任务和提醒。
- 工作机不需要公网 IP，由工作机主动连接云端。

## 注册主流程

| 步骤 | 用户动作 | 系统行为 | 输出 |
|---|---|---|---|
| 1 | 输入邮箱并提交注册 | 规范化邮箱，执行速率和重复检查 | 验证挑战或安全提示 |
| 2 | 完成邮箱验证 | 创建或激活用户主体 | 可登录状态 |
| 3 | 登录 | 创建受控 Web 会话 | 当前用户和会话信息 |
| 4 | 首次进入 | 引导设置时区、语言和基础偏好 | 完整个人上下文 |

具体采用验证码链接、一次性验证码还是其他登录方式尚未确认；实现前必须形成身份技术决策。

## 工作机绑定主流程

| 步骤 | 用户动作 | 云端行为 | 工作机行为 |
|---|---|---|---|
| 1 | 在工作机发起绑定 | 创建短期、单次绑定挑战 | 显示绑定指引 |
| 2 | 在已登录手机端确认目标设备 | 校验用户、挑战、有效期和设备摘要 | 等待确认结果 |
| 3 | 用户批准绑定 | 建立用户与工作机关系 | 保存受保护的设备身份材料 |
| 4 | 工作机连接云端 | 校验绑定状态，建立连接会话 | 注册能力并开始心跳 |
| 5 | 用户查看设备 | 返回显示名、状态、最近在线和能力摘要 | 不上传无关本地信息 |

## 解绑流程

1. 用户在手机或受信任工作机发起解绑。
2. 云端撤销设备关系并终止活跃连接。
3. 未开始租约停止投递；执行中任务进入协调或阻塞状态。
4. 工作机清理 ArcMind 连接材料，但不删除用户本地工作文件。
5. 历史任务继续显示当时执行设备，不能因解绑丢失审计关系。

## 异常流程

| 异常 | 系统行为 | 用户可见结果 |
|---|---|---|
| 邮箱已存在 | 不泄露不必要的存在性细节，进入安全登录或恢复引导 | 统一提示 |
| 验证挑战过期 | 拒绝并允许受限重发 | 明确过期和重试入口 |
| 绑定挑战被重复使用 | 拒绝第二次绑定并记录安全事件 | 提示重新发起 |
| 绑定时设备摘要变化 | 中止绑定 | 要求重新确认设备 |
| 已解绑设备重连 | 拒绝连接和任务获取 | 工作机提示需要重新绑定 |
| 手机会话已撤销 | 关闭相关实时连接并拒绝后续操作 | 返回重新登录状态 |

## 人工确认点

- 工作机显示名、操作系统摘要和绑定发起时间。
- 绑定的是哪一个用户主体。
- 解绑对正在执行任务的处理方式。
- 身份恢复是否会撤销既有 Web 会话和工作机关系。

## 待确认点

- 首版具体登录和邮箱验证方式。
- 是否允许多个手机 Web 会话同时在线。
- 身份恢复后是否默认撤销所有工作机。
- 工作机绑定确认是否必须在手机端完成。
