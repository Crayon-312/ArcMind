# 文档附件规则

本目录只保存 ArcMind V2 正式文档需要、适合 Git（版本控制工具）管理的附件。

- 优先使用文本、SVG（可缩放矢量图）等可审查格式。
- 音视频、安装包、数据库、模型和大体积生成物默认不提交。
- 必须提交大文件时，先在对应任务舱记录用途、体积、更新频率和存储决策。
- 附件文件名使用稳定、可读的英文短名称，引用附件的 Markdown 文档负责说明中文含义。
